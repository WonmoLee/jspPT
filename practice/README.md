#### 반응형 웹 연습

##오라클 12C 사용자 생성
```SQL

ALTER SESSION SET "_ORACLE_SCRIPT"=TRUE;  
CREATE USER practice IDENTIFIED BY bitc5600;

GRANT CREATE SESSION TO practice;
GRANT CREATE TABLESPACE TO practice;
GRANT CREATE TABLE TO practice;
GRANT CREATE SEQUENCE TO practice;
--GRANT SELECT, INSERT, DELETE, UPDATE ON practice.PLAYER TO practice;
ALTER USER practice DEFAULT TABLESPACE USERS QUOTA UNLIMITED ON USERS;
```

##테이블 생성
```SQL

CREATE TABLE PRODUCTS(
	ID NUMBER PRIMARY KEY,
    NAME VARCHAR2(100) NOT NULL UNIQUE,
    TYPE VARCHAR2(100) NOT NULL,
    PRICE NUMBER NOT NULL,
    COUNT NUMBER NOT NULL
);

```

##시퀀스 생성
```SQL

CREATE SEQUENCE PRODUCTS_SEQ
START WITH 1 INCREMENT BY 1;

```

##튜플 생성
```SQL

INSERT INTO PRODUCTS(ID, NAME, TYPE, PRICE, COUNT) VALUES(PRODUCTS_SEQ.NEXTVAL, '사과', '과일', 2000, 200);
INSERT INTO PRODUCTS(ID, NAME, TYPE, PRICE, COUNT) VALUES(PRODUCTS_SEQ.NEXTVAL, '배', '과일', 3000, 100);
INSERT INTO PRODUCTS(ID, NAME, TYPE, PRICE, COUNT) VALUES(PRODUCTS_SEQ.NEXTVAL, '지우개', '문구', 1000, 400);
INSERT INTO PRODUCTS(ID, NAME, TYPE, PRICE, COUNT) VALUES(PRODUCTS_SEQ.NEXTVAL, '샤프', '문구', 5000, 100);
INSERT INTO PRODUCTS(ID, NAME, TYPE, PRICE, COUNT) VALUES(PRODUCTS_SEQ.NEXTVAL, '청바지', '옷', 30000, 50);
INSERT INTO PRODUCTS(ID, NAME, TYPE, PRICE, COUNT) VALUES(PRODUCTS_SEQ.NEXTVAL, '셔츠', '옷', 9000, 200);

```